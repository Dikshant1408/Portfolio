# Required: Get your free API key from https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: Change the AI model (must be available on OpenRouter)
OPENROUTER_MODEL=mistralai/mistral-7b-instruct:free

# Optional: SQLite database file path
DB_PATH=chat_history.db

# Optional: Comma-separated list of allowed CORS origins
# Use * to allow all origins (not recommended for production)
ALLOWED_ORIGINS=http://localhost:3000,https://dikshantrajput.dev
